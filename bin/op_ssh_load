#!/usr/bin/env bash

set -euo pipefail

op get item --vault Personal SSH \
  | jq --raw-output '.details.sections[] | select(.title=="keys").fields[] | select(.t=="base64 encoded private key").v' \
  | base64 --decode

